# Fardarter Drive™ v6.14 — Public Offer Truth and External-Input Readiness

## Purpose

Fardarter Drive™ v6.14 reconciles the active GitHub offer in Issue #133 with the verified v6.13 unified control plane. The Issue #133 body is the mutable current public operating surface. Historical comments and repository receipts remain append-only evidence.

This control does not deploy an application, contact a requester, send email, infer consent, create an order, confirm payment, start work, activate capacity, select legal terms, or append a canonical event.

## Public operating surface

The exact post-merge Issue #133 title is:

```text
GitHub Control Tower Audit + Fardarter Drive™ v6.14 — verified production / public-safe intake open
```

The exact body is stored at:

```text
templates/fardarter-drive-public-offer-v6-14.md
```

Its marker is:

```text
<!-- jp-fardarter-public-offer-v6-14 -->
```

Its SHA-256 is:

```text
369338fda0fe9e2236eecd68a1321635e025e85365eb701e10ee1d8bb3c405e0
```

The body may be updated exactly once after the reviewed implementation merges. Later corrections require a new versioned receipt; historical comments must not be rewritten.

## Repository and production distinction

```text
Repository base at preparation       d284510c179a4ac38a0459c18cea23b474973605
Unified control                       v6.13 / RECONCILED
Production control                    v6.12 / RECONCILED
Deployed application source           88bd8dc89a0bd95f42c7e1a21dccc56fe9dc7334
Verified Netlify deploy               6a6ba0366ebec6650d843ac3
Immutable route readback              18/18
Exact-body matches                    18/18
Repository/live relationship          CONTROL_AHEAD_OF_DEPLOYED_APPLICATION_SOURCE
```

Issue #133 may accurately state that a verified production application exists. It must also state that repository controls are ahead of the deployed application source. Updating the offer issue does not promote or redeploy the repository head.

Any future production promotion remains owned by the separate Fardarter Drive Live Watch/provider-evidence rail and requires exact target locking, provider deploy evidence, authoritative immutable URL, complete route verification, rollback preservation, and separate human authorization.

## External-input readiness

The offer state is `OPEN_FOR_VERIFIED_FIT_CHECKS`.

A requester must use an exact-prefix `[Audit request]:` issue and provide public-safe form data. When the required GitHub-contact permission is checked:

- the issue author is the verified identity;
- the exact request issue is the authorized initial channel;
- the repository-native v6.9 workflow is the sole automatic first-response writer;
- no more than one automatic public-safe clarification may be posted;
- email is not required and is never scraped or guessed.

The open fit-check count is dynamic interest evidence only. It is not an order count and does not reserve capacity.

## Canonical, consent, capacity, and money lock

```text
Event head                  1
Reconciliation              1
SCOPE_DRAFTED               1
HUMAN_ACCEPTED              0
ACTIVE                      0
Event 2                     absent
Consent package             NO_PACKAGE
Consent decision            AWAITING_COUNTERPARTY_EVIDENCE
Planning slots              1,000
Effective ACTIVE ceiling    100
ACTIVE deliveries           0
Active headroom             100
Capacity override           INACTIVE_NO_RECEIPT
Orders                      0
Verified gross              $0
Settled cash                $0
Received-cash gate          PAID_SETTLED
```

Fit approval, contact permission, a first response, a private work package, a file, a label, a comment, a reaction, message delivery, or silence does not create consent, HUMAN_ACCEPTED, an order, a contract, payment, revenue, work start, ACTIVE use, or event 2.

## Privacy

Private Drive state is `CONNECTED_PRIVATE` with 17 known continuity documents. The public offer may state the private status and document count, but it must not expose Google Drive URLs, file IDs, folder IDs, buyer identities, consent text, signatures, authentication evidence, provider records, banking data, counsel notes, or private deliberations.

## Verification

Run:

```bash
npm run fardarter:public-offer:check
npm run revenue:verify
```

The verifier checks the locked manifest/schema, body digest, v6.13/v6.12 links, GitHub contact contract, canonical state, consent, capacity, money, public privacy boundary, stale-claim rejection, CI read-only behavior, and package integration.

## Decision

Before merge: `READY_FOR_REVIEWED_PUBLIC_RECONCILIATION`.

After merge and exact Issue #133 update: append a completion receipt rather than rewriting this prepared manifest. No canonical or commercial state advances automatically.
