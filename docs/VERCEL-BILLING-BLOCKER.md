# Historical Vercel record — retired from intended path

## Current authority

```text
Platform classification  VERCEL_RETIRED_NOT_PLANNED
Primary platform          GITHUB_PRIMARY_INTENDED
Publication target        GITHUB_PAGES_PUBLICATION_PENDING_VERIFICATION
Repair intended           false
```

The account-blocked Vercel checks are preserved as historical evidence. JP has selected GitHub as the intended platform for source, review, Actions verification, Artifacts/Releases/Packages, and Pages publication.

Do not pay, reconnect, create, or repair a Vercel account or project for the current project path. A future Vercel use would require a separately authorized explicit exception and new evidence.

The older blocker note below is retained only as historical context.

---

# Vercel Billing Blocker

Vercel checks may remain blocked until the account billing issue is paid and cleared.

## Historical operator note

```txt
I need to pay the billing account. I will pay it when I can get it paid. Then Vercel will check. Until then.
```

## Historical status

```txt
Vercel: blocked by account/billing state
Netlify: former primary GRIPLOOM deploy rail
GitHub Actions: source verification rail
```

## Historical meaning

A Vercel failure that points to account or billing status was treated as a hosting-account blocker, not a GRIPLOOM code failure, unless Vercel build logs showed a TypeScript, Next.js, package, or runtime error.

## Retired operator path

The former repair sequence is no longer current work:

```txt
1. Pay or resolve the Vercel billing account issue.
2. Wait for Vercel account checks to clear.
3. Re-run or push a new commit to trigger Vercel checks.
4. Compare Vercel result against GitHub Actions and deployment status.
```

## Current rule

```txt
GitHub is the rail. Vercel is preserved history, not planned work.
```
