name: MATADATA Pathfinder V2.4 Upgrade

on:
  pull_request:
    branches: [main]
    types: [synchronize]

permissions:
  contents: write

concurrency:
  group: matadata-v24-upgrade
  cancel-in-progress: true

jobs:
  upgrade:
    if: github.head_ref == 'agent/matadata-openai-pathfinder-v2-2'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out the staged Pathfinder branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 1
          show-progress: false

      - name: Apply and verify the V2.4 textarea normalization repair
        shell: bash
        run: |
          set -o pipefail
          python -u tools/matadata-pathfinder/v24/upgrade.py 2>&1 | tee /tmp/matadata-v24-upgrade.log

      - name: Preserve upgrade diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: matadata-pathfinder-v24-upgrade-diagnostics
          path: /tmp/matadata-v24-upgrade.log
          if-no-files-found: warn
          retention-days: 1

      - name: Remove one-time upgrade machinery and commit V2.4
        shell: bash
        run: |
          set -euo pipefail
          rm -rf tools/matadata-pathfinder/v24
          rm -f .github/workflows/matadata-v24-upgrade.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git status --short
          git diff --cached --stat
          git commit -m 'Upgrade MATADATA Pathfinder to V2.4 canonical textarea verification'
          git push origin HEAD:${{ github.head_ref }}
