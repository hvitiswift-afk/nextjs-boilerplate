name: MATADATA OpenAI Form Pathfinder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dormant-during-two-file-update:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo dormant
